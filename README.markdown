Folder Git
==========

Run a Git command in several repositories.

Test
----

Requires `shunit2`.

    git submodule update --init
    make test

Install
-------

    sudo make install

`make test` depends on `shunit2`.

Usage
-----

    fgit --help
